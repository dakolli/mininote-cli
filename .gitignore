# OS cruft
.DS_Store

# Local secrets (never committed)
.env

# GoReleaser build output
dist/

# Locally built binary (root only, do not ignore cmd/mininote/)
/mininote

# Local dev tooling (never published)
/Makefile

# Internal dev docs & generator inputs (never published)
AGENTS.md
.ignore
intro.json
api-key-forbidden.txt
